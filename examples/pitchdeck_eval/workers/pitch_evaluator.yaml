name: pitch_evaluator
description: Score a single Markdown pitch deck using the shared rubric.
sandboxes:
  input:
    path: ./input
    mode: ro
    allowed_suffixes:
      - .md
      - .txt
    max_bytes: 300000
