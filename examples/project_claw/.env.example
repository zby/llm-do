# Path to knowledge base notes directory (relative to project root or absolute)
TRIAGE_KB_PATH=docs/notes

# qmd collection name for semantic search
TRIAGE_KB_COLLECTION=notes

# Directory for timestamped snapshots of external sources (default is gitignored)
TRIAGE_SNAPSHOT_DIR=.cache/snapshots

# Comma-separated list of repos to filter notifications (empty = all repos)
# TRIAGE_GITHUB_REPOS=pydantic/pydantic-ai,anthropics/anthropic-sdk-python
TRIAGE_GITHUB_REPOS=
