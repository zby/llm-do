name: whiteboard_orchestrator
description: Orchestrate whiteboard → project plan generation for all images in input/
model: null
allow_workers: ["whiteboard_planner"]
sandbox:
  paths:
    input:
      root: ./input
      mode: ro
      suffixes: [".jpg", ".jpeg", ".png"]
      max_file_bytes: 20971520 # 20 MB
    plans:
      root: ./plans
      mode: rw
      suffixes: [".md"]
tool_rules:
  sandbox.write:
    allowed: true
    approval_required: true
  worker.call:
    allowed: true
    approval_required: false
